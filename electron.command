#!/usr/bin/env zsh
# =============================================================
# AI Companion 桌面版启动 (macOS 可双击执行)
# 启动前端 + Electron (Electron 自行管理后端进程)
# =============================================================
cd "$(dirname "$0")" || exit 1
./start.sh --electron
