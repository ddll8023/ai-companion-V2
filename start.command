#!/usr/bin/env zsh
# =============================================================
# AI Companion 启动 (macOS 可双击执行)
# 启动 后端 + 前端，浏览器访问模式
# =============================================================
cd "$(dirname "$0")" || exit 1
./start.sh
