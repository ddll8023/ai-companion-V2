# AI Companion 后端环境配置
# 以下配置用于本地开发，Electron 正式环境由桌面运行时传入

# 服务地址
HOST=127.0.0.1
PORT=18080

# 数据库路径（开发模式）
DATABASE_URL=sqlite:///./data/ai_companion.db

# 日志级别
LOG_LEVEL=INFO

# CORS 允许的源（开发模式添加 Vite 端口）
CORS_ORIGINS=["http://127.0.0.1:9753","http://localhost:9753"]
